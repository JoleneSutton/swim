# Get the example shapefiles

Imports a stored shapefile as a SpatVector (terra).

## Usage

``` r
sw_internal_shapefiles(x)
```

## Arguments

- x:

  Only option is to return all

## Value

Returns a list of SpatVectors x\<-sw_internal_shapefiles('all') x
